<!-- TODO: Add styling to timer, text, and buttons -->
    
<Window x:Class="PomodoroTimer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PomodoroTimer"
        mc:Ignorable="d"
        Title="MainWindow" Height="250" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="timerDisplay" Text="25:00" FontSize="48" HorizontalAlignment="Center" />
        <TextBlock x:Name="modeDisplay" Text="Work" FontSize="24" HorizontalAlignment="Center" Grid.Row="1" />
        <Button x:Name="startStopButton" Content="Start" Click="startStopButton_Click" HorizontalAlignment="Center" Grid.Row="2" />
        <Button x:Name="resetButton" Content="Reset" Click="resetButton_Click" HorizontalAlignment="Center" Grid.Row="3" />
    </Grid>
</Window>
